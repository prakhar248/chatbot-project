# Optional: only needed when the frontend is hosted (e.g. GitHub Pages).
# Leave unset for local dev (Vite proxy will forward /ollama to localhost:11434).
#
# Recommended: use the proxy backend (e.g. on Render) so the tunnel URL can change without redeploying the frontend:
# VITE_CHAT_API_URL=https://your-proxy.onrender.com
#
# Or point directly at a tunnel (URL changes when you restart the tunnel):
# VITE_OLLAMA_API_URL=https://your-tunnel-id.trycloudflare.com
